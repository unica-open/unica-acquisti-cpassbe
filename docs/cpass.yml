swagger: '2.0'
info:
  title: Contabilità passiva
  description: API per il backend della suite di gestione ciclo passivo dei grandi enti.
  version: 2.1.0
host: 'localhost:8080'
schemes:
  - http
  - https
basePath: /cpassbe/api/v1
produces:
  - application/json

# PATHS
$include: ./paths/index.yml
# PARAMETERS
$include: ./parameters/index.yml
# DEFINITIONS
$include: ./definitions/index.yml
