C:\Program Files\java\jre1.8.0_141

scaricare visual studio 
scaricarsi eclipse 

impostare su variabili di ambiente JAVA_HOME       C:\Program Files\java\jdk1.6.0_45
impostare su variabili di path   aggiungere        C:\Program Files\java\jdk1.6.0_45

installare il project per la vpn csi con certificato
con credenziali 
portal.csi.it 
antonino.benedetto@consulenti.csi.it
installare l'app di autenticazione

gogs in PL https://pro-logic.it/gogs/user/login 
benedettoa antoninob

Importarsi la cartella .ssh e gestire le chiavi

--da eclipse clonare il progetto


DB 
creare il DB cpass

lanciare  la prima volta 
CREATE EXTENSION IF NOT EXISTS plpgsql WITH SCHEMA pg_catalog;
		COMMENT ON EXTENSION plpgsql IS 'PL/pgSQL procedural language';

		CREATE EXTENSION IF NOT EXISTS adminpack WITH SCHEMA pg_catalog;
		COMMENT ON EXTENSION adminpack IS 'Administration and managements tools';

		CREATE EXTENSION IF NOT EXISTS pg_stat_statements WITH SCHEMA public;
		COMMENT ON EXTENSION pg_stat_statements IS 'Tracking execution statistics';

		CREATE EXTENSION IF NOT EXISTS pg_buffercache WITH SCHEMA public;
		COMMENT ON EXTENSION pg_buffercache IS 'Examining the shared buffer cache in real time';

		CREATE EXTENSION IF NOT EXISTS pgcrypto WITH SCHEMA public;
		COMMENT ON EXTENSION pgcrypto IS 'cryptographic functions';

		CREATE EXTENSION IF NOT EXISTS "uuid-ossp" WITH SCHEMA public;
		COMMENT ON EXTENSION "uuid-ossp" IS 'generate universally unique identifiers (UUIDs)';
		
		
fare l'import		


beehive ssh nodes-files get dv-wf0123-coto-cpass.site02.nivolapiemonte.it  /appserv/jboss/awf230/part001cpassnode01/modules/modules.tgz  ./modules.tgz

MAVEN x wildfly-build/23.0.0.Final
https://mvnrepository.com/artifact/org.wildfly/wildfly-build/23.0.0.Final