Settore:
  type: object
  properties:
    codice:
      type: string
    validita:
      type: string
    dataCancellazione:
      type: string
      format: date-time      
    dataCreazione:
      type: string
      format: date-time
    dataModifica:
      type: string
      format: date-time
    dataValiditaFine:
      type: string
      format: date-time
    dataValiditaInizio:
      type: string
      format: date-time      
    descrizione:
      type: string
    sede:
      type: string
    indirizzoSettorePrincipale:
      type: string
    numCivicoSettorePrincipale:
      type: string
    localitaSettorePrincipale:
      type: string
    provinciaSettorePrincipale:
      type: string
    capSettorePrincipale:
      type: string
    contattoSettorePrincipale:
      type: string
    emailSettorePrincipale:
      type: string
    telefonoSettorePrincipale:
      type: string
    all:
      type: string	    
    ente:
      type: object
      $ref: '#/definitions/Ente'
    id:
      type: string
      format: uuid
    listSettore:
      type: array
      items:  
        $ref: '#/definitions/Settore'
    optlock:
      type: string
      format: uuid
    rup:
      type: object
      $ref: '#/definitions/Utente'
    settoreIndirizzos:
      type: array
      items:  
        $ref: '#/definitions/SettoreIndirizzo'
    settorePadre:
      type: object
      $ref: '#/definitions/Settore'
    tipoSettore:
      type: object
      $ref: '#/definitions/TipoSettore'
    uffici:
      type: array
      items:  
        $ref: '#/definitions/Ufficio'
    cdcs:
      type: array
      items:  
        $ref: '#/definitions/Cdc'        
    utenteCancellazione:
      type: string
    utenteCreazione:
      type: string
    utenteModifica:
      type: string
    utenteSettoreDefault:
      type: integer
    firma:
      type: string
    aooActas:
      type: array
      items:  
        $ref: '#/definitions/AooActa' 