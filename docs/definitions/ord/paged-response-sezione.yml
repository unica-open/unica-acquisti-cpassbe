PagedResponseSezione:
  type: object
  properties:
    list:
      type: array
      items:
        $ref: '#/definitions/Sezione'
    currentPage:
      type: integer
      format: int64
    totalPages:
      type: integer
      format: int64
    totalElements:
      type: integer
      format: int64