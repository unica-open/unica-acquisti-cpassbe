ImpegnoOrdine:
  type: object
  properties:
    dataCancellazione:
      type: string
      format: date-time
    dataCreazione:
      type: string
      format: date-time
    dataModifica:
      type: string
      format: date-time
    id:
      type: string
      format: uuid
    impegno:
      type: object
      $ref: '#/definitions/Impegno'
    impegnoAnno:
      type: integer
    impegnoAnnoEsercizio:
      type: integer
    impegnoNumero:
      type: integer
    impegnoProgressivo:
      type: integer
    importo:
      type: number
    optlock:
      type: string
      format: uuid
    rigaOrdine:
      type: object
      $ref: '#/definitions/RigaOrdine'
    utenteCancellazione:
      type: string
    utenteCreazione:
      type: string
    utenteModifica:
      type: string
