ImpegnoEvasione:
  type: object
  properties:
    causaleSospensioneEvasione:
      type: object
      $ref: '#/definitions/CausaleSospensioneEvasione'
    dataCancellazione:
      type: string
      format: date-time
    dataCreazione:
      type: string
      format: date-time
    dataModifica:
      type: string
      format: date-time
    dataSospensione:
      type: string
      format: date-time
    id:
      type: string
      format: uuid
    impegno:
      type: object
      $ref: '#/definitions/Impegno'
    impegnoAnno:
      type: integer
    impegnoAnnoEsercizio:
      type: integer
    impegnoNumero:
      type: integer
    impegnoOrdine:
      type: object
      $ref: '#/definitions/ImpegnoOrdine'
    impegnoProgressivo:
      type: integer
    importoLiquidato:
      type: number
    importoRipartito:
      type: number
    importoSospeso:
      type: number
    optlock:
      type: string
      format: uuid
    rigaEvasione:
      type: object
      $ref: '#/definitions/RigaEvasione'
    subimpegnoEvasiones:
      type: array
      items:  
        $ref: '#/definitions/SubimpegnoEvasione'
    totaleRipartibile:
      type: number
    utenteCancellazione:
      type: string
    utenteCreazione:
      type: string
    utenteModifica:
      type: string
