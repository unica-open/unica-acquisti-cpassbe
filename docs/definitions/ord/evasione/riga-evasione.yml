RigaEvasione:
  type: object
  properties:
    aliquoteIva:
      type: object
      $ref: '#/definitions/AliquoteIva'
    dataCancellazione:
      type: string
      format: date-time
    dataCreazione:
      type: string
      format: date-time
    dataModifica:
      type: string
      format: date-time
    destinatarioEvasione:
      type: object
      $ref: '#/definitions/DestinatarioEvasione'
    documentoTrasportoRiga:
      type: object
      $ref: '#/definitions/DocumentoTrasportoRiga'
    id:
      type: string
      format: uuid
    impegnoEvasiones:
      type: array
      items:  
        $ref: '#/definitions/ImpegnoEvasione'
    importoTotale:
      type: number
    listinoFornitore:
      type: object
      $ref: '#/definitions/ListinoFornitore'
    oggettiSpesa:
      type: object
      $ref: '#/definitions/OggettiSpesa'
    optlock:
      type: string
      format: uuid
    prezzoUnitario:
      type: number
    progressivo:
      type: integer
    rigaOrdine:
      type: object
      $ref: '#/definitions/RigaOrdine'
    statoElOrdine:
      type: object
      $ref: '#/definitions/StatoElOrdine'
    totaleEvaso:
      type: number
    utenteCancellazione:
      type: string
    utenteCreazione:
      type: string
    utenteModifica:
      type: string
