PagedResponseIntervento:
  type: object
  properties:
    list:
      type: array
      items:
        $ref: '#/definitions/Intervento'
    currentPage:
      type: integer
      format: int64
    totalPages:
      type: integer
      format: int64
    totalElements:
      type: integer
      format: int64
