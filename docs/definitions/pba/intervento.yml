Intervento:
  type: object
  properties:
    acquistoVariato:
      type: object
      $ref: '#/definitions/AcquistoVariato'
    annoAvvio:
      type: integer
    ausa:
      type: object
      $ref: '#/definitions/Ausa'
    avviato:
      type: boolean
    capofila:
      type: boolean
    cpv:
      type: object
      $ref: '#/definitions/Cpv'
    cui:
      type: string
    cup:
      type: string
    dataAvviato:
      type: string
      format: date-time
    dataCancellazione:
      type: string
      format: date-time
    dataCreazione:
      type: string
      format: date-time
    dataModifica:
      type: string
      format: date-time
    dataRifiuto:
      type: string
      format: date-time
    dataValidazione:
      type: string
      format: date-time
    dataVisto:
      type: string
      format: date-time
    dataVistoRagioneria:
      type: string
      format: date-time
    descrizioneAcquisto:
      type: string
    durataMesi:
      type: integer
    elementiDipendenti:
      type: boolean
    esenteCup:
      type: integer
    flagCuiNonGenerato:
      type: integer
    descrizioneUltimoStato:
      type: string 
    id:
      type: string
      format: uuid
    interventoCapofila:
      type: object
      $ref: '#/definitions/Intervento'
    interventoCopia:
      type: object
      $ref: '#/definitions/Intervento'
    interventoCopiaTipo:
      type: string
    interventoImportiCopiaTipo:
      type: string
    listCpv:
      type: array
      items:  
        $ref: '#/definitions/Cpv'
    listInterventoAltriDati:
      type: array
      items:  
        $ref: '#/definitions/InterventoAltriDati'
    listInterventoImporti:
      type: array
      items:  
        $ref: '#/definitions/InterventoImporti'
    listMetadatiFunzione:
      type: array
      items:  
        $ref: '#/definitions/MetadatiFunzione'
    lottoFunzionale:
      type: boolean
    modalitaAffidamento:
      type: object
      $ref: '#/definitions/ModalitaAffidamento'
    motivazioneNonRiproposto:
      type: string
    motivazioneRifiutoRagioneria:
      type: string
    motivazioneRifiuto:
      type: string
    motivazioneRiportaInBozza:
      type: string      
    nuovoAffidamento:
      type: integer
    nuts:
      type: object
      $ref: '#/definitions/Nuts'
    optlock:
      type: string
      format: uuid
    priorita:
      type: object
      $ref: '#/definitions/Priorita'
    programma:
      type: object
      $ref: '#/definitions/Programma'
    ricompresoCui:
      type: string
    ricompresoTipo:
      type: object
      $ref: '#/definitions/RicompresoTipo'
    risorsaIdCapitalePrivato:
      type: integer
    selNonRip:
      type: string
    settore:
      type: object
      $ref: '#/definitions/Settore'
    settoreInterventi:
      type: object
      $ref: '#/definitions/SettoreInterventi'
    statiInterventos:
      type: array
      items:  
        $ref: '#/definitions/StatiIntervento'
    stato:
      type: object
      $ref: '#/definitions/Stato'
    statoXStorico:
      type: string
    storicoInterventoRups:
      type: array
      items:  
        $ref: '#/definitions/StoricoInterventoRup'
    utenteAvviato:
      type: object
      $ref: '#/definitions/Utente'
    utenteCancellazione:
      type: string
    utenteCreazione:
      type: string
    utenteModifica:
      type: string
    utenteRifiuto:
      type: object
      $ref: '#/definitions/Utente'
    utenteRup:
      type: object
      $ref: '#/definitions/Utente'
    utenteValidazione:
      type: object
      $ref: '#/definitions/Utente'
    utenteVisto:
      type: object
      $ref: '#/definitions/Utente'
    utenteVistoRagioneria:
      type: object
      $ref: '#/definitions/Utente'
    versioneDefinitiva:
      type: boolean
    versioneDefinitivaStr:
      type: string
    esenteIva:
      type: string  
    vistoRagioneria:
      type: boolean
    vistoRagioneriaStr:
      type: string
    tipoProceduraPba:
      type: object
      $ref: '#/definitions/TipoProceduraPba'
    interventoCigs:
      type: array
      items:  
        $ref: '#/definitions/InterventoCig'
    presenteInAltroProgramma:
      type: boolean
    presenteInAltroProgrammaFuturo:
      type: boolean